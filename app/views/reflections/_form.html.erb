<%= form_for(@reflection) do |f| %>
  <% if @reflection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reflection.errors.count, "error") %> prohibited this reflection from being saved:</h2>

      <ul>
      <% @reflection.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
